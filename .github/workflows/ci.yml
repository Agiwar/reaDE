name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    name: lint, type-check, test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: reade
          POSTGRES_PASSWORD: reade  # pragma: allowlist secret — throwaway test-container credential
          POSTGRES_DB: reade
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U reade"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      mysql:
        image: mysql:8.4
        env:
          MYSQL_USER: reade
          MYSQL_PASSWORD: reade  # pragma: allowlist secret — throwaway test-container credential
          MYSQL_DATABASE: reade
          MYSQL_ROOT_PASSWORD: reade  # pragma: allowlist secret
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping -h 127.0.0.1 -uroot -preade --silent"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 30
    env:
      READE_TEST_POSTGRES_HOST: 127.0.0.1
      READE_TEST_MYSQL_HOST: 127.0.0.1
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Install project + dev dependencies (locked)
        run: uv sync --extra dev --frozen

      - name: Lint (ruff)
        run: make lint

      - name: Type check (mypy strict)
        run: make type-check

      - name: Security scan (bandit)
        run: make security

      - name: Import contracts (import-linter)
        run: make imports

      - name: Tests (pytest)
        run: make test

      - name: Integration tests ran (not skipped)
        run: set -o pipefail && uv run pytest -m integration -rs | grep -E "[1-9][0-9]* passed"

      - name: End-to-end example
        run: uv run python examples/end_to_end.py

      - name: Typed-config example
        run: uv run python examples/config_typed.py

      - name: Server-backed example
        run: uv run python examples/db_typed.py

      - name: Render-contract example
        run: uv run python examples/sql_render.py

      - name: Validation-rules example
        run: uv run python examples/validation_rules.py

      - name: Config module coverage gate (>=90%)
        run: uv run pytest --cov=reade.config --cov-fail-under=90 -q

      - name: Db module coverage gate (>=90%)
        run: uv run pytest --cov=reade.db --cov-fail-under=90 -q

      - name: Sql module coverage gate (>=90%)
        run: uv run pytest --cov=reade.sql --cov-fail-under=90 -q

      - name: Data_io module coverage gate (>=90%)
        run: uv run pytest --cov=reade.data_io --cov-fail-under=90 -q

      - name: Validation module coverage gate (>=90%)
        run: uv run pytest --cov=reade.validation --cov-fail-under=90 -q

      - name: Global coverage gate (>=90%)
        run: uv run pytest --cov=src --cov-fail-under=90 -q
